/**
 * 1. Create firebase project
 * 2. Create web app
 * 3. npm install firebase
 * 4. Save firebase config js file and export default app
 * 5. BUild> Authentication > Get started > Enable sign un methods (providers)
 * 6. Create sign up and login route
 **/

/**
* 1. Create a context provider
* 2. Create a context and set provider
* 3. set the children props
* 4. set context value
* 5. set the provider
**/